# backend/requirements.txt
# Python 3.11+ requerido
# Instalar con: pip install -r requirements.txt

# API Framework
fastapi>=0.115.5
uvicorn[standard]>=0.32.1

# Cálculo estadístico y numérico
numpy>=2.2.0
scipy>=1.15.0
pandas>=2.2.3

# HTTP client (para Gemini API y Open-Meteo)
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx  # también usado en tests

# Utilidades
python-multipart==0.0.12  # para upload de archivos en FastAPI

# Scraping (para update_data.py — actualización automática de datos)
requests==2.32.3          # HTTP clásico para elo_scraper y fifa_scraper
beautifulsoup4==4.12.3    # HTML parsing (eloratings.net, fifa.com)
# lxml removido — se usa html.parser (built-in) por compatibilidad con Python 3.14
cloudscraper==1.2.71      # Bypass Cloudflare (transfermarkt.com) — opcional
